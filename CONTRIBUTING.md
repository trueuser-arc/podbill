# Contributing

podbill stays deliberately small — one Python file + ffmpeg. Bug reports and
small, focused PRs are very welcome; big features should start as an issue so
we can talk scope before you spend an evening on it.

Run the tests before opening a PR: `python3 -m pytest tests/ -q` (all green).
Please don't include real client data in issues, fixtures, or tests.
